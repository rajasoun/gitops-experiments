---
apiVersion: apps/v1
kind: Service
metadata:
  name: podinfo
  namespace: podinfo
  labels:
    app: podinfo
spec:
  type: ClusterIP
  ports:
  - port: 9898
    targetPort: 9898
    name: http
  selector:
    app: podinfo
---